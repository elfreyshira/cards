import _ from 'lodash'

// addUndo(roller)
// func(addUndo) returns [cardObj, undoChain]
function getLeastSimilarObj (func) {
  const [cardObj, undoChain] = func()
}

export default getLeastSimilarObj
